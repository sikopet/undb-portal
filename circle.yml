machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t scbd/undb.cbd.int .

test:
  override:
    - docker run -d -p 8000:8000 scbd/undb.cbd.int; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:8000
