machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t scbd/soi-portal .

test:
  override:
    - docker run -d -p 8000:8000 scbd/soi-portal; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:8000
